// Code generated by protoc-gen-grpc-react-native. DO NOT EDIT.
// Generated from: grpcbin.proto

class BinaryWriter {
  private buffer: Uint8Array = new Uint8Array(1024);
  private position: number = 0;

  private ensureCapacity(needed: number): void {
    if (this.position + needed > this.buffer.length) {
      const newBuffer = new Uint8Array(this.buffer.length * 2);
      newBuffer.set(this.buffer);
      this.buffer = newBuffer;
    }
  }

  private writeVarint(value: number): void {
    while (value > 127) {
      this.ensureCapacity(1);
      this.buffer[this.position++] = (value & 127) | 128;
      value >>>= 7;
    }
    this.ensureCapacity(1);
    this.buffer[this.position++] = value & 127;
  }

  private writeVarintBigInt(value: bigint): void {
    while (value > 127n) {
      this.ensureCapacity(1);
      this.buffer[this.position++] = Number(value & 127n) | 128;
      value >>= 7n;
    }
    this.ensureCapacity(1);
    this.buffer[this.position++] = Number(value & 127n);
  }

  writeDouble(fieldNumber: number, value: number): void {
    this.writeVarint((fieldNumber << 3) | 1); // wire type 1
    this.ensureCapacity(8);
    const view = new DataView(this.buffer.buffer, this.buffer.byteOffset + this.position, 8);
    view.setFloat64(0, value, true); // little endian
    this.position += 8;
  }

  writeFloat(fieldNumber: number, value: number): void {
    this.writeVarint((fieldNumber << 3) | 5); // wire type 5
    this.ensureCapacity(4);
    const view = new DataView(this.buffer.buffer, this.buffer.byteOffset + this.position, 4);
    view.setFloat32(0, value, true); // little endian
    this.position += 4;
  }

  writeInt32(fieldNumber: number, value: number): void {
    this.writeVarint((fieldNumber << 3) | 0); // wire type 0
    this.writeVarint(value);
  }

  writeInt64(fieldNumber: number, value: bigint): void {
    this.writeVarint((fieldNumber << 3) | 0); // wire type 0
    this.writeVarintBigInt(value);
  }

  writeUint32(fieldNumber: number, value: number): void {
    this.writeVarint((fieldNumber << 3) | 0); // wire type 0
    this.writeVarint(value);
  }

  writeUint64(fieldNumber: number, value: bigint): void {
    this.writeVarint((fieldNumber << 3) | 0); // wire type 0
    this.writeVarintBigInt(value);
  }

  writeBool(fieldNumber: number, value: boolean): void {
    this.writeVarint((fieldNumber << 3) | 0); // wire type 0
    this.writeVarint(value ? 1 : 0);
  }

  writeString(fieldNumber: number, value: string): void {
    this.writeVarint((fieldNumber << 3) | 2); // wire type 2
    const bytes = this.encodeString(value);
    this.writeVarint(bytes.length);
    this.ensureCapacity(bytes.length);
    this.buffer.set(bytes, this.position);
    this.position += bytes.length;
  }

  writeBytes(fieldNumber: number, value: Uint8Array): void {
    this.writeVarint((fieldNumber << 3) | 2); // wire type 2
    this.writeVarint(value.length);
    this.ensureCapacity(value.length);
    this.buffer.set(value, this.position);
    this.position += value.length;
  }

  writeMessage(fieldNumber: number, value: any): void {
    this.writeVarint((fieldNumber << 3) | 2); // wire type 2
    const bytes = value.serializeBinary();
    this.writeVarint(bytes.length);
    this.ensureCapacity(bytes.length);
    this.buffer.set(bytes, this.position);
    this.position += bytes.length;
  }

  writeEnum(fieldNumber: number, value: number): void {
    this.writeVarint((fieldNumber << 3) | 0); // wire type 0
    this.writeVarint(value);
  }

  private encodeString(str: string): Uint8Array {
    // Manual UTF-8 encoding (React Native compatible)
    const bytes: number[] = [];
    for (let i = 0; i < str.length; i++) {
      let codePoint = str.charCodeAt(i);
      if (codePoint >= 0xD800 && codePoint <= 0xDBFF && i + 1 < str.length) {
        const next = str.charCodeAt(i + 1);
        if (next >= 0xDC00 && next <= 0xDFFF) {
          codePoint = 0x10000 + ((codePoint - 0xD800) << 10) + (next - 0xDC00);
          i++;
        }
      }
      if (codePoint < 0x80) {
        bytes.push(codePoint);
      } else if (codePoint < 0x800) {
        bytes.push(0xC0 | (codePoint >> 6), 0x80 | (codePoint & 0x3F));
      } else if (codePoint < 0x10000) {
        bytes.push(0xE0 | (codePoint >> 12), 0x80 | ((codePoint >> 6) & 0x3F), 0x80 | (codePoint & 0x3F));
      } else {
        bytes.push(0xF0 | (codePoint >> 18), 0x80 | ((codePoint >> 12) & 0x3F), 0x80 | ((codePoint >> 6) & 0x3F), 0x80 | (codePoint & 0x3F));
      }
    }
    return new Uint8Array(bytes);
  }

  getResultBuffer(): Uint8Array {
    return this.buffer.slice(0, this.position);
  }
}

class BinaryReader {
  buffer: Uint8Array;
  position: number = 0;

  constructor(data: Uint8Array) {
    this.buffer = data;
  }

  readVarint(): number {
    let result = 0;
    let shift = 0;
    while (this.position < this.buffer.length) {
      const byte = this.buffer[this.position++];
      result |= (byte & 127) << shift;
      if ((byte & 128) === 0) break;
      shift += 7;
    }
    return result;
  }

  readVarintBigInt(): bigint {
    let result = 0n;
    let shift = 0n;
    while (this.position < this.buffer.length) {
      const byte = BigInt(this.buffer[this.position++]);
      result |= (byte & 127n) << shift;
      if ((byte & 128n) === 0n) break;
      shift += 7n;
    }
    return result;
  }

  readDouble(): number {
    const view = new DataView(this.buffer.buffer, this.buffer.byteOffset + this.position, 8);
    this.position += 8;
    return view.getFloat64(0, true); // little endian
  }

  readFloat(): number {
    const view = new DataView(this.buffer.buffer, this.buffer.byteOffset + this.position, 4);
    this.position += 4;
    return view.getFloat32(0, true); // little endian
  }

  readInt32(): number {
    return this.readVarint();
  }

  readInt64(): bigint {
    return this.readVarintBigInt();
  }

  readUint32(): number {
    return this.readVarint();
  }

  readUint64(): bigint {
    return this.readVarintBigInt();
  }

  readBool(): boolean {
    return this.readVarint() !== 0;
  }

  readString(): string {
    const length = this.readVarint();
    const bytes = this.buffer.slice(this.position, this.position + length);
    this.position += length;
    return this.decodeString(bytes);
  }

  private decodeString(bytes: Uint8Array): string {
    // Manual UTF-8 decoding (React Native compatible)
    let result = '';
    let i = 0;
    while (i < bytes.length) {
      const byte1 = bytes[i++];
      if (byte1 < 0x80) {
        result += String.fromCharCode(byte1);
      } else if ((byte1 & 0xE0) === 0xC0) {
        const byte2 = bytes[i++] & 0x3F;
        result += String.fromCharCode(((byte1 & 0x1F) << 6) | byte2);
      } else if ((byte1 & 0xF0) === 0xE0) {
        const byte2 = bytes[i++] & 0x3F;
        const byte3 = bytes[i++] & 0x3F;
        result += String.fromCharCode(((byte1 & 0x0F) << 12) | (byte2 << 6) | byte3);
      } else if ((byte1 & 0xF8) === 0xF0) {
        const byte2 = bytes[i++] & 0x3F;
        const byte3 = bytes[i++] & 0x3F;
        const byte4 = bytes[i++] & 0x3F;
        const codePoint = ((byte1 & 0x07) << 18) | (byte2 << 12) | (byte3 << 6) | byte4;
        result += String.fromCharCode(0xD800 + ((codePoint - 0x10000) >> 10), 0xDC00 + ((codePoint - 0x10000) & 0x3FF));
      }
    }
    return result;
  }

  readBytes(): Uint8Array {
    const length = this.readVarint();
    const bytes = this.buffer.slice(this.position, this.position + length);
    this.position += length;
    return bytes;
  }

  readMessage<T>(deserialize: (data: Uint8Array) => T): T {
    const length = this.readVarint();
    const bytes = this.buffer.slice(this.position, this.position + length);
    this.position += length;
    return deserialize(bytes);
  }

  readEnum(): number {
    return this.readVarint();
  }

  readInt32Fixed(): number {
    const bytes = this.buffer.slice(this.position, this.position + 4);
    this.position += 4;
    const view = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);
    return view.getInt32(0, true); // little-endian
  }

  readInt64Fixed(): bigint {
    const bytes = this.buffer.slice(this.position, this.position + 8);
    this.position += 8;
    const view = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);
    return view.getBigInt64(0, true); // little-endian
  }

  readUint32Fixed(): number {
    const bytes = this.buffer.slice(this.position, this.position + 4);
    this.position += 4;
    const view = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);
    return view.getUint32(0, true); // little-endian
  }

  readUint64Fixed(): bigint {
    const bytes = this.buffer.slice(this.position, this.position + 8);
    this.position += 8;
    const view = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);
    return view.getBigUint64(0, true); // little-endian
  }

  skipField(wireType: number): void {
    switch (wireType) {
      case 0: // Varint
        this.readVarint();
        break;
      case 1: // 64-bit
        this.position += 8;
        break;
      case 2: // Length-delimited
        const length = this.readVarint();
        this.position += length;
        break;
      case 3: // Start group (deprecated, just skip)
        // Groups are deprecated in proto3, skip without reading
        break;
      case 4: // End group (deprecated, just skip)
        // Groups are deprecated in proto3, skip without reading
        break;
      case 5: // 32-bit
        this.position += 4;
        break;
      default:
        throw new Error(`Unknown wire type: ${wireType}`);
    }
  }
}

export interface HeadersMessage {
  Metadata: Map<string, HeadersMessage_MetadataEntry>;
}

export class HeadersMessageImpl implements HeadersMessage {
  Metadata!: Map<string, HeadersMessage_MetadataEntry>;

  constructor(data?: Partial<HeadersMessage>) {
    this.Metadata = data?.Metadata ?? new Map();
  }

  serializeBinary(): Uint8Array {
    const writer = new BinaryWriter();

    for (const [key, value] of this.Metadata.entries()) {
      // Map serialization not fully implemented yet
      // Key: key, Value: value
    }
    return writer.getResultBuffer();
  }

  static deserializeBinary(data: Uint8Array): HeadersMessageImpl {
    const reader = new BinaryReader(data);
    const message = new HeadersMessageImpl();

    // Parse protobuf wire format
    while (reader.position < reader.buffer.length) {
      const tag = reader.readVarint();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 7;

      switch (fieldNumber) {
        case 1: // Metadata
          if (wireType === 2) { // Length-delimited
            const length = reader.readVarint();
            const endPos = reader.position + length;
            while (reader.position < endPos) {
              const entryLength = reader.readVarint();
              const entryEndPos = reader.position + entryLength;
              let key, value;
              while (reader.position < entryEndPos) {
                const entryTag = reader.readVarint();
                const entryFieldNumber = entryTag >>> 3;
                const entryWireType = entryTag & 7;
                if (entryFieldNumber === 1) {
                  key = reader.readString();
                } else if (entryFieldNumber === 2) {
                  value = reader.readMessage(HeadersMessage_MetadataEntryImpl.deserializeBinary);
                } else {
                  reader.skipField(entryWireType);
                }
              }
              if (key !== undefined && value !== undefined) {
                message.Metadata.set(key, value);
              }
            }
          } else {
            reader.skipField(wireType);
          }
          break;
        default:
          // Skip unknown fields
          reader.skipField(wireType);
          break;
      }
    }

    return message;
  }

  toObject(): HeadersMessage {
    return {
      Metadata: this.Metadata
    };
  }

}

export interface HeadersMessage_Values {
  Values: string[];
}

export class HeadersMessage_ValuesImpl implements HeadersMessage_Values {
  Values!: string[];

  constructor(data?: Partial<HeadersMessage_Values>) {
    this.Values = data?.Values ?? [];
  }

  serializeBinary(): Uint8Array {
    const writer = new BinaryWriter();

    for (const item of this.Values) {
      writer.writeString(1, item);
    }
    return writer.getResultBuffer();
  }

  static deserializeBinary(data: Uint8Array): HeadersMessage_ValuesImpl {
    const reader = new BinaryReader(data);
    const message = new HeadersMessage_ValuesImpl();

    // Parse protobuf wire format
    while (reader.position < reader.buffer.length) {
      const tag = reader.readVarint();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 7;

      switch (fieldNumber) {
        case 1: // Values
          if (wireType === 2) { // Length-delimited
            message.Values.push(reader.readString());
          } else {
            reader.skipField(wireType);
          }
          break;
        default:
          // Skip unknown fields
          reader.skipField(wireType);
          break;
      }
    }

    return message;
  }

  toObject(): HeadersMessage_Values {
    return {
      Values: this.Values
    };
  }

}

export interface HeadersMessage_MetadataEntry {
  Key: string;
  Value?: HeadersMessage_Values;
}

export class HeadersMessage_MetadataEntryImpl implements HeadersMessage_MetadataEntry {
  Key!: string;
  Value?: HeadersMessage_Values;

  constructor(data?: Partial<HeadersMessage_MetadataEntry>) {
    this.Key = data?.Key ?? "";
    this.Value = data?.Value ?? undefined;
  }

  serializeBinary(): Uint8Array {
    const writer = new BinaryWriter();

    if (this.Key !== "") {
      writer.writeString(1, this.Key);
    }
    if (this.Value) {
      writer.writeMessage(2, this.Value);
    }
    return writer.getResultBuffer();
  }

  static deserializeBinary(data: Uint8Array): HeadersMessage_MetadataEntryImpl {
    const reader = new BinaryReader(data);
    const message = new HeadersMessage_MetadataEntryImpl();

    // Parse protobuf wire format
    while (reader.position < reader.buffer.length) {
      const tag = reader.readVarint();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 7;

      switch (fieldNumber) {
        case 1: // Key
          if (wireType === 2) { // Length-delimited
            message.Key = reader.readString();
          } else {
            reader.skipField(wireType);
          }
          break;
        case 2: // Value
          if (wireType === 2) { // Length-delimited
            message.Value = reader.readMessage(HeadersMessage_ValuesImpl.deserializeBinary);
          } else {
            reader.skipField(wireType);
          }
          break;
        default:
          // Skip unknown fields
          reader.skipField(wireType);
          break;
      }
    }

    return message;
  }

  toObject(): HeadersMessage_MetadataEntry {
    return {
      Key: this.Key,
      Value: this.Value
    };
  }

}

export interface SpecificErrorRequest {
  Code: number;
  Reason: string;
}

export class SpecificErrorRequestImpl implements SpecificErrorRequest {
  Code!: number;
  Reason!: string;

  constructor(data?: Partial<SpecificErrorRequest>) {
    this.Code = data?.Code ?? 0;
    this.Reason = data?.Reason ?? "";
  }

  serializeBinary(): Uint8Array {
    const writer = new BinaryWriter();

    if (this.Code !== 0) {
      writer.writeUint32(1, this.Code);
    }
    if (this.Reason !== "") {
      writer.writeString(2, this.Reason);
    }
    return writer.getResultBuffer();
  }

  static deserializeBinary(data: Uint8Array): SpecificErrorRequestImpl {
    const reader = new BinaryReader(data);
    const message = new SpecificErrorRequestImpl();

    // Parse protobuf wire format
    while (reader.position < reader.buffer.length) {
      const tag = reader.readVarint();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 7;

      switch (fieldNumber) {
        case 1: // Code
          if (wireType === 0) { // Varint
            message.Code = reader.readUint32();
          } else if (wireType === 5) { // 32-bit
            message.Code = reader.readUint32Fixed();
          } else {
            reader.skipField(wireType);
          }
          break;
        case 2: // Reason
          if (wireType === 2) { // Length-delimited
            message.Reason = reader.readString();
          } else {
            reader.skipField(wireType);
          }
          break;
        default:
          // Skip unknown fields
          reader.skipField(wireType);
          break;
      }
    }

    return message;
  }

  toObject(): SpecificErrorRequest {
    return {
      Code: this.Code,
      Reason: this.Reason
    };
  }

}

export interface EmptyMessage {
}

export class EmptyMessageImpl implements EmptyMessage {

  constructor(data?: Partial<EmptyMessage>) {
  }

  serializeBinary(): Uint8Array {
    const writer = new BinaryWriter();

    return writer.getResultBuffer();
  }

  static deserializeBinary(data: Uint8Array): EmptyMessageImpl {
    const reader = new BinaryReader(data);
    const message = new EmptyMessageImpl();

    // Parse protobuf wire format
    while (reader.position < reader.buffer.length) {
      const tag = reader.readVarint();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 7;

      switch (fieldNumber) {
        default:
          // Skip unknown fields
          reader.skipField(wireType);
          break;
      }
    }

    return message;
  }

  toObject(): EmptyMessage {
    return {
    };
  }

}

export interface DummyMessage {
  FString: string;
  FStrings: string[];
  FInt32: number;
  FInt32S: number[];
  FEnum: DummyMessage_Enum;
  FEnums: DummyMessage_Enum[];
  FSub?: DummyMessage_Sub;
  FSubs: DummyMessage_Sub[];
  FBool: boolean;
  FBools: boolean[];
  FInt64: bigint;
  FInt64S: bigint[];
  FBytes: Uint8Array;
  FBytess: Uint8Array[];
  FFloat: number;
  FFloats: number[];
}

export class DummyMessageImpl implements DummyMessage {
  FString!: string;
  FStrings!: string[];
  FInt32!: number;
  FInt32S!: number[];
  FEnum!: DummyMessage_Enum;
  FEnums!: DummyMessage_Enum[];
  FSub?: DummyMessage_Sub;
  FSubs!: DummyMessage_Sub[];
  FBool!: boolean;
  FBools!: boolean[];
  FInt64!: bigint;
  FInt64S!: bigint[];
  FBytes!: Uint8Array;
  FBytess!: Uint8Array[];
  FFloat!: number;
  FFloats!: number[];

  constructor(data?: Partial<DummyMessage>) {
    this.FString = data?.FString ?? "";
    this.FStrings = data?.FStrings ?? [];
    this.FInt32 = data?.FInt32 ?? 0;
    this.FInt32S = data?.FInt32S ?? [];
    this.FEnum = data?.FEnum ?? 0;
    this.FEnums = data?.FEnums ?? [];
    this.FSub = data?.FSub ?? undefined;
    this.FSubs = data?.FSubs ?? [];
    this.FBool = data?.FBool ?? false;
    this.FBools = data?.FBools ?? [];
    this.FInt64 = data?.FInt64 ?? 0n;
    this.FInt64S = data?.FInt64S ?? [];
    this.FBytes = data?.FBytes ?? new Uint8Array(0);
    this.FBytess = data?.FBytess ?? [];
    this.FFloat = data?.FFloat ?? 0.0;
    this.FFloats = data?.FFloats ?? [];
  }

  serializeBinary(): Uint8Array {
    const writer = new BinaryWriter();

    if (this.FString !== "") {
      writer.writeString(1, this.FString);
    }
    for (const item of this.FStrings) {
      writer.writeString(2, item);
    }
    if (this.FInt32 !== 0) {
      writer.writeInt32(3, this.FInt32);
    }
    for (const item of this.FInt32S) {
      writer.writeInt32(4, item);
    }
    if (this.FEnum !== 0) {
      writer.writeEnum(5, this.FEnum);
    }
    for (const item of this.FEnums) {
      writer.writeEnum(6, item);
    }
    if (this.FSub) {
      writer.writeMessage(7, this.FSub);
    }
    for (const item of this.FSubs) {
      writer.writeMessage(8, item);
    }
    if (this.FBool !== false) {
      writer.writeBool(9, this.FBool);
    }
    for (const item of this.FBools) {
      writer.writeBool(10, item);
    }
    if (this.FInt64 !== 0n) {
      writer.writeInt64(11, this.FInt64);
    }
    for (const item of this.FInt64S) {
      writer.writeInt64(12, item);
    }
    if (this.FBytes.length > 0) {
      writer.writeBytes(13, this.FBytes);
    }
    for (const item of this.FBytess) {
      writer.writeBytes(14, item);
    }
    if (this.FFloat !== 0.0) {
      writer.writeFloat(15, this.FFloat);
    }
    for (const item of this.FFloats) {
      writer.writeFloat(16, item);
    }
    return writer.getResultBuffer();
  }

  static deserializeBinary(data: Uint8Array): DummyMessageImpl {
    const reader = new BinaryReader(data);
    const message = new DummyMessageImpl();

    // Parse protobuf wire format
    while (reader.position < reader.buffer.length) {
      const tag = reader.readVarint();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 7;

      switch (fieldNumber) {
        case 1: // FString
          if (wireType === 2) { // Length-delimited
            message.FString = reader.readString();
          } else {
            reader.skipField(wireType);
          }
          break;
        case 2: // FStrings
          if (wireType === 2) { // Length-delimited
            message.FStrings.push(reader.readString());
          } else {
            reader.skipField(wireType);
          }
          break;
        case 3: // FInt32
          if (wireType === 0) { // Varint
            message.FInt32 = reader.readInt32();
          } else if (wireType === 5) { // 32-bit
            message.FInt32 = reader.readInt32Fixed();
          } else {
            reader.skipField(wireType);
          }
          break;
        case 4: // FInt32S
          if (wireType === 2) { // Length-delimited (packed)
            const length = reader.readVarint();
            const endPos = reader.position + length;
            while (reader.position < endPos) {
              message.FInt32S.push(reader.readInt32());
            }
          } else {
            // Non-packed encoding - single item
            if (wireType === 0) { message.FInt32S.push(reader.readInt32()); }
          }
          break;
        case 5: // FEnum
          if (wireType === 0) { // Varint
            message.FEnum = reader.readEnum();
          } else {
            reader.skipField(wireType);
          }
          break;
        case 6: // FEnums
          if (wireType === 2) { // Length-delimited (packed)
            const length = reader.readVarint();
            const endPos = reader.position + length;
            while (reader.position < endPos) {
              message.FEnums.push(reader.readEnum());
            }
          } else {
            // Non-packed encoding - single item
            if (wireType === 0) { message.FEnums.push(reader.readEnum()); }
          }
          break;
        case 7: // FSub
          if (wireType === 2) { // Length-delimited
            message.FSub = reader.readMessage(DummyMessage_SubImpl.deserializeBinary);
          } else {
            reader.skipField(wireType);
          }
          break;
        case 8: // FSubs
          if (wireType === 2) { // Length-delimited
            message.FSubs.push(reader.readMessage(DummyMessage_SubImpl.deserializeBinary));
          } else {
            reader.skipField(wireType);
          }
          break;
        case 9: // FBool
          if (wireType === 0) { // Varint
            message.FBool = reader.readBool();
          } else {
            reader.skipField(wireType);
          }
          break;
        case 10: // FBools
          if (wireType === 2) { // Length-delimited (packed)
            const length = reader.readVarint();
            const endPos = reader.position + length;
            while (reader.position < endPos) {
              message.FBools.push(reader.readBool());
            }
          } else {
            // Non-packed encoding - single item
            if (wireType === 0) { message.FBools.push(reader.readBool()); }
          }
          break;
        case 11: // FInt64
          if (wireType === 0) { // Varint
            message.FInt64 = reader.readInt64();
          } else if (wireType === 1) { // 64-bit
            message.FInt64 = reader.readInt64Fixed();
          } else {
            reader.skipField(wireType);
          }
          break;
        case 12: // FInt64S
          if (wireType === 2) { // Length-delimited (packed)
            const length = reader.readVarint();
            const endPos = reader.position + length;
            while (reader.position < endPos) {
              message.FInt64S.push(reader.readInt64());
            }
          } else {
            // Non-packed encoding - single item
            if (wireType === 0) { message.FInt64S.push(reader.readInt64()); }
          }
          break;
        case 13: // FBytes
          if (wireType === 2) { // Length-delimited
            message.FBytes = reader.readBytes();
          } else {
            reader.skipField(wireType);
          }
          break;
        case 14: // FBytess
          if (wireType === 2) { // Length-delimited
            message.FBytess.push(reader.readBytes());
          } else {
            reader.skipField(wireType);
          }
          break;
        case 15: // FFloat
          if (wireType === 5) { // 32-bit
            message.FFloat = reader.readFloat();
          } else {
            reader.skipField(wireType);
          }
          break;
        case 16: // FFloats
          if (wireType === 2) { // Length-delimited (packed)
            const length = reader.readVarint();
            const endPos = reader.position + length;
            while (reader.position < endPos) {
              message.FFloats.push(reader.readFloat());
            }
          } else {
            // Non-packed encoding - single item
            if (wireType === 5) { message.FFloats.push(reader.readFloat()); }
          }
          break;
        default:
          // Skip unknown fields
          reader.skipField(wireType);
          break;
      }
    }

    return message;
  }

  toObject(): DummyMessage {
    return {
      FString: this.FString,
      FStrings: this.FStrings,
      FInt32: this.FInt32,
      FInt32S: this.FInt32S,
      FEnum: this.FEnum,
      FEnums: this.FEnums,
      FSub: this.FSub,
      FSubs: this.FSubs,
      FBool: this.FBool,
      FBools: this.FBools,
      FInt64: this.FInt64,
      FInt64S: this.FInt64S,
      FBytes: this.FBytes,
      FBytess: this.FBytess,
      FFloat: this.FFloat,
      FFloats: this.FFloats
    };
  }

}

export interface DummyMessage_Sub {
  FString: string;
}

export class DummyMessage_SubImpl implements DummyMessage_Sub {
  FString!: string;

  constructor(data?: Partial<DummyMessage_Sub>) {
    this.FString = data?.FString ?? "";
  }

  serializeBinary(): Uint8Array {
    const writer = new BinaryWriter();

    if (this.FString !== "") {
      writer.writeString(1, this.FString);
    }
    return writer.getResultBuffer();
  }

  static deserializeBinary(data: Uint8Array): DummyMessage_SubImpl {
    const reader = new BinaryReader(data);
    const message = new DummyMessage_SubImpl();

    // Parse protobuf wire format
    while (reader.position < reader.buffer.length) {
      const tag = reader.readVarint();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 7;

      switch (fieldNumber) {
        case 1: // FString
          if (wireType === 2) { // Length-delimited
            message.FString = reader.readString();
          } else {
            reader.skipField(wireType);
          }
          break;
        default:
          // Skip unknown fields
          reader.skipField(wireType);
          break;
      }
    }

    return message;
  }

  toObject(): DummyMessage_Sub {
    return {
      FString: this.FString
    };
  }

}

export enum DummyMessage_Enum {
  DummyMessage_ENUM_0 = 0,
  DummyMessage_ENUM_1 = 1,
  DummyMessage_ENUM_2 = 2,
}

export interface IndexReply {
  Description: string;
  Endpoints: IndexReply_Endpoint[];
}

export class IndexReplyImpl implements IndexReply {
  Description!: string;
  Endpoints!: IndexReply_Endpoint[];

  constructor(data?: Partial<IndexReply>) {
    this.Description = data?.Description ?? "";
    this.Endpoints = data?.Endpoints ?? [];
  }

  serializeBinary(): Uint8Array {
    const writer = new BinaryWriter();

    if (this.Description !== "") {
      writer.writeString(1, this.Description);
    }
    for (const item of this.Endpoints) {
      writer.writeMessage(2, item);
    }
    return writer.getResultBuffer();
  }

  static deserializeBinary(data: Uint8Array): IndexReplyImpl {
    const reader = new BinaryReader(data);
    const message = new IndexReplyImpl();

    // Parse protobuf wire format
    while (reader.position < reader.buffer.length) {
      const tag = reader.readVarint();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 7;

      switch (fieldNumber) {
        case 1: // Description
          if (wireType === 2) { // Length-delimited
            message.Description = reader.readString();
          } else {
            reader.skipField(wireType);
          }
          break;
        case 2: // Endpoints
          if (wireType === 2) { // Length-delimited
            message.Endpoints.push(reader.readMessage(IndexReply_EndpointImpl.deserializeBinary));
          } else {
            reader.skipField(wireType);
          }
          break;
        default:
          // Skip unknown fields
          reader.skipField(wireType);
          break;
      }
    }

    return message;
  }

  toObject(): IndexReply {
    return {
      Description: this.Description,
      Endpoints: this.Endpoints
    };
  }

}

export interface IndexReply_Endpoint {
  Path: string;
  Description: string;
}

export class IndexReply_EndpointImpl implements IndexReply_Endpoint {
  Path!: string;
  Description!: string;

  constructor(data?: Partial<IndexReply_Endpoint>) {
    this.Path = data?.Path ?? "";
    this.Description = data?.Description ?? "";
  }

  serializeBinary(): Uint8Array {
    const writer = new BinaryWriter();

    if (this.Path !== "") {
      writer.writeString(1, this.Path);
    }
    if (this.Description !== "") {
      writer.writeString(2, this.Description);
    }
    return writer.getResultBuffer();
  }

  static deserializeBinary(data: Uint8Array): IndexReply_EndpointImpl {
    const reader = new BinaryReader(data);
    const message = new IndexReply_EndpointImpl();

    // Parse protobuf wire format
    while (reader.position < reader.buffer.length) {
      const tag = reader.readVarint();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 7;

      switch (fieldNumber) {
        case 1: // Path
          if (wireType === 2) { // Length-delimited
            message.Path = reader.readString();
          } else {
            reader.skipField(wireType);
          }
          break;
        case 2: // Description
          if (wireType === 2) { // Length-delimited
            message.Description = reader.readString();
          } else {
            reader.skipField(wireType);
          }
          break;
        default:
          // Skip unknown fields
          reader.skipField(wireType);
          break;
      }
    }

    return message;
  }

  toObject(): IndexReply_Endpoint {
    return {
      Path: this.Path,
      Description: this.Description
    };
  }

}

